{
  "name": "habla-tutor-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy that lets Habla chat with Claude Haiku.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.114.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
